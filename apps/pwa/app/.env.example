# Base API path for the Rust server.
# - Dev default can be /api with Vite proxy
# - Direct mode example: http://localhost:3000/api
VITE_API_BASE=/api

# Optional: local dev proxy target for Vite (used in vite.config.ts)
# KRUSTY_SERVER_ORIGIN=http://localhost:3000
