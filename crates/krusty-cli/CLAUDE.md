# krusty-cli

Terminal UI application built with ratatui.

## Structure
- `main.rs` - CLI entry point (clap subcommands)
- `tui/` - Terminal UI (see tui/CLAUDE.md)

See root CLAUDE.md for build commands.
